# Basic Auth 凭据文件（被 Caddyfile import）。
# 使用方法：
#   1. 复制本文件为 auth.caddy（scripts/init-env.sh 会自动复制）
#   2. 生成密码哈希（把「你的密码」替换为实际密码）：
#        docker run --rm caddy:2-alpine caddy hash-password --plaintext '你的密码'
#   3. 把下面 admin 换成你的用户名，哈希原样粘贴（无需任何转义）
# auth.caddy 含密码哈希，已在 .gitignore 中，切勿提交
basic_auth {
	admin $2a$14$替换为caddy hash-password生成的完整哈希
}
